# ── Web Framework ─────────────────────────────────────────────
fastapi==0.136.0
uvicorn
gunicorn
python-multipart==0.0.20
python-dotenv==1.2.2
pydantic==2.13.3

# ── Document Processing (Docling pulls in torch, transformers, etc.) ──
docling==2.91.0

# ── CrewAI Agents (pulls in litellm, groq client, etc.) ───────
crewai>=0.121.0
crewai-tools>=0.38.0
langchain-openai>=0.3.0
litellm>=1.40.0

# ── File Generation ────────────────────────────────────────────
pandas>=2.0.0
openpyxl>=3.1.0          # pandas Excel engine (xls/xlsx)
python-docx>=1.1.0       # Word document generation
docxtpl>=0.18.0          # Jinja2 template-based Word generation (available for branded templates)
docx2pdf>=0.1.8          # PDF via DOCX (requires MS Word on macOS/Windows, LibreOffice on Linux)
python-pptx>=1.0.0       # PowerPoint generation

# ── Object Store (optional — upload generated files to S3/compatible) ──
boto3>=1.34.0
